# AI Employee - Dependencies
# Python Dependencies for Qwen Code CLI, Ollama, and DashScope Integration

# ===========================================
# Core Dependencies (Required)
# ===========================================
watchdog>=3.0.0
pathlib2>=2.3.7
python-dotenv>=1.0.0

# ===========================================
# AI Providers (Choose One or More)
# ===========================================

# Option 1: Qwen Code CLI (RECOMMENDED - Full-featured agent)
# Install separately from: https://chat.qwen.ai
# Or via npm: npm install -g @qwen-code/cli
# No Python package needed - uses subprocess to call CLI

# Option 2: Ollama (FREE, Local)
# No API key required, runs locally on your machine
# Download from: https://ollama.com/download
ollama>=0.1.0

# Option 3: DashScope (API key required)
# Higher quality models, but requires internet and API key
qwen-agent>=0.0.34
dashscope>=1.25.0

# ===========================================
# Silver Tier: Email Integration
# ===========================================

# Gmail API (for Gmail Watcher)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.0.0

# ===========================================
# Silver Tier: WhatsApp Integration
# ===========================================

# Playwright for browser automation (WhatsApp Web)
playwright>=1.40.0

# After installation, run: playwright install chromium

# ===========================================
# Silver Tier: LinkedIn Integration
# ===========================================

# LinkedIn auto-posting (uses Playwright - already listed above)
# No additional dependencies needed

# ===========================================
# Gold Tier: Additional Social Media
# ===========================================

# Facebook, Instagram, Twitter (uses Playwright - already listed above)
# No additional dependencies needed

# ===========================================
# Gold Tier: Audit Logging & Error Recovery
# ===========================================

# These use standard library - no additional dependencies

# ===========================================
# Development & Testing
# ===========================================

pytest>=7.4.0
pytest-cov>=4.1.0

# ===========================================
# Optional: MCP Servers (For Advanced Actions)
# ===========================================

# MCP SDK (if building MCP servers)
# mcp>=1.0.0

# ===========================================
# Installation Instructions
# ===========================================
#
# Basic Installation (All Python dependencies):
#   pip install -r requirements.txt
#   playwright install chromium
#
# Minimal Installation (Ollama only):
#   pip install watchdog python-dotenv ollama
#
# Qwen Code CLI Setup (Recommended):
#   1. Install Qwen Code CLI:
#      - Download from: https://chat.qwen.ai
#      - Or: npm install -g @qwen-code/cli
#   2. Authenticate: qwen-code login
#   3. Set in .env: AI_PROVIDER=qwen_code_cli
#
# Silver Tier Full Setup:
#   pip install -r requirements.txt
#   playwright install chromium
#   ollama pull qwen2.5:7b
#
# ===========================================
