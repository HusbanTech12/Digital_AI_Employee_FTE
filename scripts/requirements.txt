# AI Employee - Gold Tier
# Python Dependencies

# Core dependencies
watchdog>=3.0.0
pathlib2>=2.3.7

# Environment variables
python-dotenv>=1.0.0

# AI Providers (choose one or more)

# Option 1: Ollama (FREE, Local - Recommended)
ollama>=0.1.0

# Option 2: DashScope (API key required, free credits for new users)
qwen-agent>=0.0.34
dashscope>=1.25.0

# Silver Tier: Gmail watcher
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.0.0

# Silver Tier: WhatsApp watcher (Playwright-based)
playwright>=1.40.0

# Silver Tier: LinkedIn automation (uses Playwright)
# (playwright already listed above)

# Gold Tier: Social media integration
# Facebook, Instagram, Twitter (uses Playwright)

# Gold Tier: Audit logging
# (no additional dependencies)

# Gold Tier: Error recovery
# (uses standard library)

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
